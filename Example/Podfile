use_frameworks!

target 'Example' do
    platform :ios, '8.0'
    target 'iOSTest' do
        inherit! :search_paths
        pod 'EasySequence', path: '../'
        
        pod 'Quick'
        pod 'Nimble'
    end
    
end

